# TLS test fixtures (deploy bundle)

PEM files copied from `proxy/core/tests/fixtures/tls` so **Docker Compose on fresh servers** can run bridge / connector / stealth-agent / zentinel **without initializing the `proxy/core` submodule**.

Do not use these certificates in production.
